import 'package:flutter/material.dart';

class TShadowStyle {
  static final verticalProductShadow = BoxShadow(
      color: Colors.grey.withOpacity(0.1),
      blurRadius: 53,
      spreadRadius: 7,
      offset: const Offset(0, 2));

  static final horizantolProductShadow = BoxShadow(
      color: Colors.grey.withOpacity(0.1),
      blurRadius: 53,
      spreadRadius: 7,
      offset: const Offset(0, 2));
}
